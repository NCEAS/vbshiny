apiVersion: v1
kind: Service
metadata:
  name: {{ include "vegbank-shinyverse.fullname" . }}
  labels:
    {{- include "vegbank-shinyverse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vegbank-shinyverse.selectorLabels" . | nindent 4 }}
